<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:EDictionary.Core.Learner"
										xmlns:tb="http://www.hardcodet.net/taskbar">

	<ContextMenu x:Shared="false" x:Key="SysTrayMenu">
		<MenuItem Header="Open EDictionary" Command="{Binding OpenMainDictionaryCommand}" />
		<MenuItem Header="{Binding NextStatus}" Command="{Binding ToggleActiveCommand}" />
		<Separator />
		<MenuItem Header="Settings" Command="{Binding OpenSettingsCommand}" />
		<MenuItem Header="About" Command="{Binding OpenAboutCommand}" />
		<MenuItem Header="Exit" Command="{Binding ExitAppCommand}" />
	</ContextMenu>

	<tb:TaskbarIcon x:Key="LearnerNotifyIcon"
		IconSource="/Icons/E-Dictionary.ico"
		ToolTipText="EDictionary Learner"
		DoubleClickCommand="{Binding OpenLearnerBalloonCommand}"
		ContextMenu="{StaticResource SysTrayMenu}" />

</ResourceDictionary>